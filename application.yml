spring:
  application:
    name: SERVER-ComingCOnfig

eureka:
  instance:
    hostname: localhost
    ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
